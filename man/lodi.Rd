% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lodi.R
\name{lodi}
\alias{lodi}
\title{Fit the LODI dynamic factor model}
\usage{
lodi(
  Y,
  K,
  niter = 4000L,
  nburn = 2000L,
  thin = 1L,
  L = 10L,
  genotype = NULL,
  seed = 1L
)
}
\arguments{
\item{Y}{Integer array of counts with dimension \eqn{N \times T \times J}
(subjects x timepoints x features). Missing values are not supported.}

\item{K}{Integer, the number of latent factors used in the fitted model.}

\item{niter}{Total number of MCMC iterations.}

\item{nburn}{Number of initial iterations discarded as burn-in.}

\item{thin}{Thinning interval: only every \code{thin}-th post-burn-in iteration
is retained.}

\item{L}{Truncation level for the stick-breaking approximation to the
Dirichlet-process mixture on \eqn{\alpha_{ij1}}.}

\item{genotype}{Optional integer vector of length \eqn{N} giving the
genotype/group label of each subject in \eqn{\{1, \dots, G\}}, used for
the genotype-specific AR(1) coefficient \eqn{\phi_g}. Default \code{NULL}
places all subjects in a single group.}

\item{seed}{Integer random seed passed to the C++ sampler.}
}
\value{
A list of posterior draws with elements
\describe{
\item{\code{Lambda}}{array \verb{nkeep x J x K}, factor loadings.}
\item{\code{eta}}{array \verb{nkeep x T x K x N}, latent factor scores.}
\item{\code{alpha}}{array \verb{nkeep x N x T x J}, per-feature drift trajectories.}
\item{\code{r}}{array \verb{nkeep x N x T}, sample-scale factors.}
\item{\code{sigma2}}{matrix \verb{nkeep x T}, per-time residual variance.}
\item{\code{rho}}{numeric vector \code{nkeep}, AR(1) coefficient of the factor process.}
\item{\code{phi_g}}{matrix \verb{nkeep x G}, genotype-specific drift AR(1) coefficients.}
\item{\code{V}}{numeric vector \code{nkeep}, drift innovation variance.}
\item{\code{niter}, \code{nburn}, \code{thin}, \code{K}, \code{L}}{sampler settings for reference.}
}
}
\description{
Runs the LODI MCMC on an \eqn{N \times T \times J} array of counts.
Given the observed counts, the sampler jointly infers the per-(subject,
time) log sample-scale factor \eqn{r_{it}}, the genotype-specific AR(1)
per-feature drift \eqn{\alpha_{ijt}} (with a Dirichlet-process mixture
prior on the initial baseline \eqn{\alpha_{ij1}}), the shared cross-feature
loading matrix \eqn{\Lambda} (identified via a lower-triangular constraint
with positive diagonal), the AR(1) latent factors \eqn{\eta_{it}}, and the
per-time residual variance \eqn{\sigma^2_t}.
}
\examples{
\donttest{
sim <- simulate_lodi(N = 20, T = 5, J = 20, K = 3, seed = 1)
fit <- lodi(sim$Y, K = 3, niter = 200, nburn = 100, thin = 2, seed = 1)
str(fit, max.level = 1)
}

}
