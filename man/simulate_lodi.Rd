% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_lodi}
\alias{simulate_lodi}
\title{Simulate a longitudinal count table from the LODI generative model}
\usage{
simulate_lodi(
  N = 20,
  T = 7,
  J = 30,
  K = 3,
  G = 1,
  phi = 0.8,
  V = 0.2,
  rho = 0.7,
  sigma2 = 0.5,
  mu_cluster = c(-4, 2, 8),
  w_cluster = c(0.5, 0.2, 0.3),
  r_mean = 0,
  r_sd = 1,
  seed = 1L
)
}
\arguments{
\item{N}{Number of subjects.}

\item{T}{Number of timepoints.}

\item{J}{Number of features.}

\item{K}{True number of latent factors used to generate the data.}

\item{G}{Number of genotype groups (subjects are split evenly).}

\item{phi}{Per-feature AR(1) coefficient (shared across genotypes here).}

\item{V}{Innovation variance of the drift process.}

\item{rho}{AR(1) coefficient of the shared factor process.}

\item{sigma2}{Global residual variance for the log-latent.}

\item{mu_cluster}{Length-3 vector of DP-mixture cluster means for
\eqn{\alpha_{ij1}}.}

\item{w_cluster}{Length-3 vector of DP-mixture cluster weights.}

\item{r_mean, r_sd}{Mean and sd of the per-(subject, time) intercept.}

\item{seed}{Random seed.}
}
\value{
A list with elements
\describe{
\item{\code{Y}}{integer array \verb{N x T x J}, the simulated counts.}
\item{\code{Y_star}}{numeric array \verb{N x T x J}, the log-latent used to
generate the counts (before flooring).}
\item{\code{Lambda},\code{eta},\code{alpha},\code{r}}{the parameters used to generate the data.}
\item{\code{genotype}}{integer vector of length \code{N} giving each subject's
genotype label in \eqn{\{1, ..., G\}}.}
\item{\code{Sigma_true}}{array \verb{J x J x T}, the true within-time cross-feature
covariance \eqn{\Lambda\Lambda^\top + \sigma^2 I} at each timepoint.}
\item{\code{settings}}{list of the simulation settings used.}
}
}
\description{
Draws a synthetic dataset matching the "Simulation 1: matched truth"
setting of Zhang, Ok, and Ni (submitted), "Bayesian Nonparametric
Longitudinal Factor Model for Microbiome Counts": rounded log-normal observation
model, three-cluster Dirichlet-process mixture initial baselines, AR(1)
per-feature drift, and AR(1) shared factors with lower-triangular
identifiable loadings.
}
\examples{
sim <- simulate_lodi(N = 10, T = 4, J = 15, K = 2, seed = 1)
dim(sim$Y)
mean(sim$Y == 0)

}
